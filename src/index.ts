import * as numbers from './numbers'
import * as objects from './objects'
import * as strings from './strings'
import * as tuples from './tuples'
import { pipe } from './utils/pipe'

export { numbers, objects, pipe, strings, tuples }
